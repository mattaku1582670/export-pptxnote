# 実装指示書：PowerPoint発表者ノート抽出Webアプリ

## 1. あなたの役割

あなたは、TypeScript・React・Office Open XML形式に精通したシニアWebアプリケーションエンジニアです。

以下の要件を満たす、実際にローカルで起動・ビルドできるWebアプリを実装してください。  
不明点があっても作業を止めず、要件から妥当な設計を判断して、完成度の高い初期版を作成してください。

---

## 2. アプリの目的

ユーザーがPowerPointファイル（`.pptx`）を選択、またはブラウザへドラッグ＆ドロップすると、各スライドの**発表者ノート**をスライド順に抽出するWebアプリを作成する。

抽出したノートは次の方法で利用できるようにする。

1. アプリ画面上で確認・編集する
2. Wordファイル（`.docx`）としてダウンロードする
3. 全文をクリップボードへコピーする
4. プレーンテキスト（`.txt`）としてダウンロードする

PowerPointのスライド画像やスライド内容をアプリ内で表示する必要はない。

---

## 3. 最重要方針

### 3.1 完全ブラウザ内処理

PowerPointファイルと抽出したノートは、サーバーへ送信しないこと。

すべての処理をブラウザ内で完結させる。

画面上に次の趣旨を明示する。

> 選択したファイルはサーバーへ送信されません。すべての処理はこのブラウザ内で行われます。

### 3.2 対象ファイル

初期版では次の形式だけを正式対応とする。

- `.pptx`

以下は対象外とし、選択された場合は分かりやすいエラーメッセージを表示する。

- `.ppt`
- パスワード保護されたPowerPoint
- 破損したファイル
- PowerPoint以外のファイル

`.pptm`は積極的に対応しなくてよい。内部構造が読み取れる場合でも、初期版では未対応として扱ってよい。

### 3.3 PowerPointを実行しない

Microsoft PowerPoint、Office、LibreOffice、外部変換サービスは使用しない。

`.pptx`をZIP形式として展開し、内部のOffice Open XMLを解析してノートを取得する。

---

## 4. 推奨技術構成

以下を基本構成として使用する。

- React
- TypeScript
- Vite
- JSZip
- docx
- ブラウザ標準の`DOMParser`
- CSS Modules、通常のCSS、またはシンプルなCSS

大規模なUIライブラリは必須ではない。  
依存関係を増やしすぎず、保守しやすい構成にする。

### インストール候補

```bash
npm create vite@latest pptx-notes-extractor -- --template react-ts
cd pptx-notes-extractor
npm install
npm install jszip docx
```

必要性が明確であれば、軽量な追加ライブラリを導入してもよい。  
ただし、PPTX解析そのものを外部APIへ依存させてはいけない。

---

## 5. 必須機能

## 5.1 ファイル入力

次の両方に対応する。

- ファイル選択ボタン
- ドラッグ＆ドロップ

ファイル入力エリアには以下を表示する。

- 対応形式：`.pptx`
- ファイルをドラッグ＆ドロップできること
- ファイル選択ボタン
- ブラウザ内処理であること

同じファイルを再選択しても処理できるようにする。

---

## 5.2 ファイル検証

処理前に最低限、以下を確認する。

- 拡張子が`.pptx`である
- ファイルサイズが0バイトではない
- ZIPとして読み込める
- `ppt/presentation.xml`が存在する

異常時は技術的な例外をそのまま表示せず、日本語で理解しやすいエラーを表示する。

例：

- 「対応しているファイル形式は.pptxです。」
- 「PowerPointファイルを読み取れませんでした。ファイルが破損しているか、パスワードで保護されている可能性があります。」
- 「このファイルにはPowerPointのプレゼンテーション情報が見つかりませんでした。」

---

## 5.3 PPTXの解析

### 重要

`notesSlide1.xml`を単純に「スライド1のノート」と決めつけてはいけない。

スライドの削除、追加、複製、並べ替えが行われている場合でも正しく処理できるように、Office Open XMLのリレーションをたどること。

### 基本的な解析手順

1. JSZipで`.pptx`を展開する
2. `ppt/presentation.xml`を解析する
3. `ppt/_rels/presentation.xml.rels`を解析する
4. `presentation.xml`内の`p:sldIdLst`に記録された順番でスライドを取得する
5. 各`p:sldId`の`r:id`から、対応するスライドXMLを特定する
6. 各スライドのリレーションファイルを解析する
7. RelationshipのTypeが`notesSlide`を示すものを探す
8. 対応する`ppt/notesSlides/notesSlide*.xml`を解析する
9. ノート本文だけを抽出する
10. スライド順の配列として保持する

### パスの例

```text
ppt/presentation.xml
ppt/_rels/presentation.xml.rels
ppt/slides/slide1.xml
ppt/slides/_rels/slide1.xml.rels
ppt/notesSlides/notesSlide1.xml
```

### Relationship Targetの解決

Relationshipの`Target`は相対パスになっている場合がある。

例：

```text
../slides/slide1.xml
../notesSlides/notesSlide1.xml
```

文字列連結だけで処理せず、`.`や`..`を解決してPPTX内部の正規化されたパスへ変換する共通関数を作成すること。

Windowsのファイルパス用APIへ依存せず、ZIP内部のパスとして `/` 区切りで扱う。

---

## 5.4 ノート本文の抽出

ノートXMLに含まれるすべての`a:t`要素を無条件に連結しないこと。

ノートスライドには次のような本文以外の情報が含まれる可能性がある。

- スライド番号
- 日付
- ヘッダー
- フッター
- スライド画像用プレースホルダー
- その他のプレースホルダー

### 抽出対象

原則として、プレースホルダー種別が`body`であるシェイプ内のテキストを抽出する。

想定構造の例：

```xml
<p:sp>
  <p:nvSpPr>
    <p:nvPr>
      <p:ph type="body"/>
    </p:nvPr>
  </p:nvSpPr>
  <p:txBody>
    <a:p>
      <a:r>
        <a:t>ノート本文</a:t>
      </a:r>
    </a:p>
  </p:txBody>
</p:sp>
```

XML名前空間の接頭辞が必ずしも固定とは限らないため、`getElementsByTagNameNS("*", "t")`など、ローカル名を利用した堅牢な解析を検討する。

### 段落と改行

- `a:p`ごとに段落として扱う
- 同一段落内の複数の`a:t`は連結する
- `a:br`がある場合は段落内改行として扱う
- 段落間は改行で区切る
- 空段落を無制限に増やさない
- 前後の不要な空白や空行を整える
- 日本語を含むUnicode文字列を正しく保持する

### 箇条書き

初期版では、箇条書きの完全な書式再現までは必須ではない。

ただし、XMLから箇条書きと明確に判断できる場合は、以下のようなプレーンテキスト表現へ変換してよい。

```text
・項目1
・項目2
```

インデントレベルを取得できる場合は、全角または半角スペースで簡易的に表現してよい。

### フォールバック

`body`プレースホルダーが存在しない特殊なPowerPointでも、ノートらしい本文を救済できるようにフォールバックを検討する。

ただし、スライド番号、日付、フッターなどが混入しないよう慎重に実装する。

優先順位：

1. `type="body"`のプレースホルダー
2. ノートXML内のテキストを持つ非ヘッダー系シェイプ
3. 何も取得できなければ「ノートなし」

---

## 5.5 スライドタイトルの取得

可能であれば、各スライドのタイトルも抽出する。

各スライドXML内で、プレースホルダー種別が次のいずれかであるシェイプをタイトル候補とする。

- `title`
- `ctrTitle`

タイトルを取得できなければ空文字でよい。

表示およびWord出力では、以下のように利用する。

```text
スライド 3：治療計画の概要
```

タイトル取得は推奨機能だが、発表者ノート抽出の正確性を優先すること。

---

## 5.6 抽出結果の表示

解析後、画面上部に次の集計情報を表示する。

- 元のファイル名
- 総スライド数
- ノートありのスライド数
- ノートなしのスライド数

各スライドはカード形式で表示する。

表示例：

```text
スライド 1：はじめに
[編集可能なテキストエリア]

スライド 2
ノートなし
```

### 必須UI

- スライド番号
- スライドタイトル（取得できた場合）
- 編集可能なテキストエリア
- ノートなしの表示
- 各スライドの文字数
- 「このスライドをコピー」ボタン
- ノートあり／なしの視認性

抽出後にユーザーがテキストを編集した場合、Word、テキスト、クリップボードへの出力には**編集後の内容**を使用する。

---

## 5.7 表示フィルター

以下の切り替えを用意する。

- すべてのスライド
- ノートありのみ
- ノートなしのみ

可能であれば、スライド番号、タイトル、ノート本文を対象にした簡易検索欄も用意する。

---

## 5.8 Wordファイル出力

`docx`ライブラリを使用し、ブラウザ上で`.docx`を生成する。

### Word文書の基本構成

```text
元ファイル名 発表者ノート

作成日時
総スライド数
ノートありのスライド数

スライド 1：タイトル
ノート本文

スライド 2：タイトル
ノート本文
```

### Word出力オプション

画面上に次のチェック項目を用意する。

- ノートのないスライドも出力する
- スライドごとに改ページする
- スライドタイトルを出力する
- 文書の先頭に集計情報を出力する

初期値：

- ノートのないスライドも出力する：オフ
- スライドごとに改ページする：オフ
- スライドタイトルを出力する：オン
- 文書の先頭に集計情報を出力する：オン

### Wordの体裁

- 文書タイトル：TitleまたはHeading 1相当
- 各スライド：Heading 2相当
- ノート本文：通常段落
- 日本語が読みやすい一般的な文字サイズ
- 過度に複雑なスタイルは不要
- 改行を保持する
- 空のノートを不必要に大きな空白として出力しない

### ファイル名

元ファイルが以下の場合：

```text
研修資料.pptx
```

出力ファイル名は次のようにする。

```text
研修資料_発表者ノート.docx
```

ファイル名に使用できない文字は安全な文字へ置換する。

---

## 5.9 全文コピー

「全文コピー」ボタンを設置する。

コピー形式の例：

```text
スライド 1：はじめに

本日は〇〇について説明します。

------------------------------

スライド 2：背景

このスライドでは背景を説明します。
```

コピー完了後は、一時的に「コピーしました」と表示する。

Clipboard APIが利用できない環境では、代替処理または分かりやすいエラーを表示する。

---

## 5.10 テキストファイル出力

編集後の内容をUTF-8の`.txt`としてダウンロードできるようにする。

ファイル名：

```text
研修資料_発表者ノート.txt
```

Word出力と同じオプションを可能な範囲で反映する。

---

## 5.11 リセット

「別のファイルを読み込む」または「リセット」ボタンを用意する。

リセット時は次を初期化する。

- 選択ファイル
- 抽出結果
- 編集済みテキスト
- エラー
- 検索語
- 表示フィルター
- 進捗表示

誤操作防止のため、編集後にリセットする場合のみ確認ダイアログを表示してもよい。

---

## 6. データ型の例

以下を参考に、明確なTypeScript型を定義する。

```ts
export interface ExtractedSlide {
  slideNumber: number;
  slidePath: string;
  title: string;
  originalNotes: string;
  editedNotes: string;
  hasNotes: boolean;
}

export interface ExtractionResult {
  fileName: string;
  slideCount: number;
  slidesWithNotes: number;
  slidesWithoutNotes: number;
  slides: ExtractedSlide[];
}

export interface ExportOptions {
  includeEmptySlides: boolean;
  pageBreakPerSlide: boolean;
  includeSlideTitles: boolean;
  includeSummary: boolean;
}
```

型名や分割方法は変更してよいが、`any`の多用は避ける。

---

## 7. 推奨ファイル構成

以下は一例であり、責務が明確であれば調整してよい。

```text
src/
├─ components/
│  ├─ FileDropZone.tsx
│  ├─ ExtractionSummary.tsx
│  ├─ SlideNotesCard.tsx
│  ├─ SlideFilterBar.tsx
│  ├─ ExportOptionsPanel.tsx
│  └─ ErrorMessage.tsx
├─ services/
│  ├─ pptxParser.ts
│  ├─ docxExporter.ts
│  └─ textExporter.ts
├─ utils/
│  ├─ xml.ts
│  ├─ zipPath.ts
│  ├─ fileName.ts
│  └─ clipboard.ts
├─ types/
│  └─ index.ts
├─ App.tsx
├─ main.tsx
└─ styles.css
```

### 責務

- `pptxParser.ts`
  - ZIP展開
  - Relationship解析
  - スライド順の取得
  - ノート抽出
  - タイトル抽出
- `docxExporter.ts`
  - Word文書生成
- `textExporter.ts`
  - TXT生成
  - 全文テキスト整形
- `zipPath.ts`
  - Relationship Targetの相対パス解決
- Reactコンポーネント
  - UI表示と状態管理

---

## 8. UI・デザイン要件

派手なデザインは不要だが、業務用途でも使える清潔で分かりやすい画面にする。

### 基本レイアウト

- 最大幅を設定した中央寄せ
- 十分な余白
- 明確な見出し
- ドラッグ＆ドロップ領域を大きくする
- ボタンの役割を明確にする
- スマートフォンでも最低限使用可能
- 主用途はPCブラウザを想定

### 状態表示

以下の状態を視覚的に分ける。

- 初期状態
- ドラッグ中
- 読み込み中
- 解析成功
- 解析失敗
- 出力処理中
- コピー成功

### アクセシビリティ

- キーボード操作可能
- `label`とフォーム部品を関連付ける
- ボタンに意味のある文言を使う
- 色だけで状態を区別しない
- エラーには`role="alert"`を検討する
- ドロップ領域はクリックでも選択可能にする

---

## 9. 進捗とパフォーマンス

大きなPowerPointでは解析に時間がかかる可能性があるため、処理中表示を行う。

表示例：

```text
PowerPointを読み込んでいます…
スライド 12 / 80 を解析しています…
Wordファイルを作成しています…
```

初期版ではWeb Workerは必須ではない。

ただし、処理ロジックをUIから分離し、将来的にWeb Workerへ移行しやすい構成にする。

非常に大きなファイルに対してブラウザが固まる可能性があるため、必要であれば警告を表示する。

例：

```text
このファイルはサイズが大きいため、処理に時間がかかる場合があります。
```

ファイルサイズ上限を設ける場合は、根拠のない極端に小さい上限にしない。  
初期案として100MB程度を警告基準にしてもよいが、強制拒否は必須ではない。

---

## 10. セキュリティ要件

- ファイルを外部へ送信しない
- 抽出したテキストを外部ログへ送信しない
- ファイル名を外部解析サービスへ送信しない
- 外部CDNへユーザーファイルを渡さない
- XML内の内容を`dangerouslySetInnerHTML`で表示しない
- XMLの文字列は通常のReactテキストとして表示する
- マクロを実行しない
- 埋め込みオブジェクトを実行しない
- PowerPoint内部の外部リンクへアクセスしない

---

## 11. エッジケース

最低限、以下を想定して実装する。

1. ノートが1件もないPowerPoint
2. 一部のスライドだけノートがある
3. スライドが並べ替えられている
4. スライドが複製されている
5. ノートに複数段落がある
6. ノートに日本語と英数字が混在する
7. ノートに絵文字や特殊記号がある
8. ノートに空段落がある
9. タイトルのないスライド
10. ノートXMLがないスライド
11. リレーションファイルがないスライド
12. Relationship Targetに`..`が含まれる
13. ファイル名に複数のピリオドがある
14. ファイル名に日本語が含まれる
15. 同じファイルを連続で選択する
16. 解析途中で例外が起こる
17. Word出力直前にノートが編集されている
18. ノート本文にHTMLのような文字列が含まれる
19. スライド数が多い
20. ZIPではあるがPowerPointではないファイル

1つのスライドの解析失敗でアプリ全体を必ず失敗させる必要はない。  
可能であれば、そのスライドを「解析できませんでした」として残し、他のスライドは表示する。

ただし、プレゼンテーション全体の構造が読み取れない場合は処理を中止する。

---

## 12. テスト要件

テスト可能なロジックをReactコンポーネントから分離する。

最低限、以下の単体テストを作成する。

### パス解決

- `ppt/slides/`と`../notesSlides/notesSlide1.xml`
- `ppt/`と`slides/slide1.xml`
- `./slide1.xml`
- 複数の`..`
- 余分な`.`

### XML解析

- Relationship IDからTargetを取得できる
- プレゼンテーションのスライド順を取得できる
- `body`プレースホルダーだけを抽出できる
- スライド番号やフッターを除外できる
- 複数段落を改行付きで抽出できる
- 複数の`a:t`を同一段落として連結できる
- タイトルを取得できる
- ノートなしを正しく判定できる

### 出力

- 編集後テキストがWord出力に反映される
- 空ノート除外オプションが動作する
- スライドごとの改ページが動作する
- ファイル名を安全に生成できる
- TXTがUTF-8で生成される

テストフレームワークはVitestを推奨する。

```bash
npm install -D vitest
```

テスト用PPTXをリポジトリに含める必要はない。  
XML文字列を使った単体テストを中心にしてよい。

---

## 13. README要件

`README.md`を作成し、以下を記載する。

- アプリの概要
- 主な機能
- 対応形式
- 非対応形式
- ブラウザ内処理であること
- セットアップ方法
- 開発サーバー起動方法
- ビルド方法
- テスト方法
- 技術構成
- PPTX解析の概要
- 既知の制限
- 静的ホスティング方法

コマンド例：

```bash
npm install
npm run dev
npm run build
npm run test
```

---

## 14. デプロイ要件

Viteの静的ビルドとして、以下のような環境へ配置できること。

- GitHub Pages
- Vercel
- Netlify
- 一般的な静的Webサーバー

バックエンドは作成しない。

GitHub Pagesへ配置する場合に備え、Viteの`base`設定が変更しやすいようにする。

---

## 15. 初期版では実装しなくてよいもの

以下はスコープ外とする。

- `.ppt`の解析
- PowerPointスライドの画像表示
- スライドのサムネイル生成
- PowerPointの編集
- ノート内画像の抽出
- 音声、動画、埋め込みファイルの抽出
- フォント、文字色、太字などの完全再現
- 表、数式、SmartArtの完全再現
- クラウドストレージ連携
- ログイン機能
- ファイル履歴
- サーバー保存
- 複数ファイルの一括処理
- PDF出力
- AIによる要約や文章修正
- 古い`.ppt`から`.pptx`への変換

---

## 16. 実装の優先順位

以下の順番で実装する。

### フェーズ1：基盤

1. Vite＋React＋TypeScriptの初期化
2. 型定義
3. JSZipでPPTXを読み込む
4. ZIP内部パス解決関数
5. XML解析ユーティリティ

### フェーズ2：PPTX解析

1. スライド順の取得
2. スライドXMLの特定
3. ノートXMLの特定
4. `body`プレースホルダーから本文抽出
5. スライドタイトル抽出
6. 集計結果生成

### フェーズ3：UI

1. ファイル選択
2. ドラッグ＆ドロップ
3. 読み込み状態
4. エラー表示
5. 抽出結果一覧
6. テキスト編集
7. フィルター
8. リセット

### フェーズ4：出力

1. 全文コピー
2. TXT出力
3. DOCX出力
4. 出力オプション

### フェーズ5：品質

1. 単体テスト
2. エッジケース対応
3. README
4. ビルド確認
5. UI調整

---

## 17. 完成条件

以下をすべて満たした時点で初期版を完成とする。

- [ ] `.pptx`をファイル選択できる
- [ ] `.pptx`をドラッグ＆ドロップできる
- [ ] ファイルが外部へ送信されない
- [ ] スライド順をリレーションから正しく取得する
- [ ] 各スライドに対応するノートを正しく特定する
- [ ] ノート本文用プレースホルダーを優先して抽出する
- [ ] スライド番号、日付、フッターの混入を抑制する
- [ ] 段落と改行がある程度維持される
- [ ] ノートのないスライドを判定できる
- [ ] 抽出結果を画面上で編集できる
- [ ] 編集後の内容を全文コピーできる
- [ ] 編集後の内容をTXTで保存できる
- [ ] 編集後の内容をDOCXで保存できる
- [ ] Word出力オプションが動作する
- [ ] 日本語ファイル名と日本語ノートを扱える
- [ ] 不正ファイルで分かりやすいエラーを表示する
- [ ] `npm run build`が成功する
- [ ] `npm run test`が成功する
- [ ] READMEがある

---

## 18. Codexへの作業指示

まず既存リポジトリの内容を確認すること。

### リポジトリが空の場合

上記要件を満たすVite＋React＋TypeScriptプロジェクトを新規作成し、必要なファイルをすべて実装する。

### 既存プロジェクトがある場合

既存の技術構成、コードスタイル、ディレクトリ構成を確認し、無理に全面置換せず、要件を満たす形で統合する。

### 作業中の原則

- 実装だけでなく、実際にコマンドを実行して確認する
- TypeScriptエラーを残さない
- ESLintが存在する場合は重大なエラーを残さない
- ビルドを実行する
- テストを実行する
- 不要なデバッグログを残さない
- 秘密情報を追加しない
- 外部APIキーを要求しない
- 仮の疑似コードだけで終了しない
- TODOだけを残して完成扱いにしない

### 最終報告に含める内容

実装終了後、以下を簡潔に報告する。

1. 実装した機能
2. 主なファイル
3. PPTX解析方式
4. 実行した確認コマンド
5. テスト結果
6. ビルド結果
7. 既知の制限
8. ローカルでの起動方法

---

## 19. 実装上の補足

### XMLパーサーエラーの確認

`DOMParser`は不正XMLでも例外を投げず、`parsererror`要素を返すことがある。

XML解析時は`parsererror`の有無を確認し、独自のエラーへ変換する。

### Relationship Typeの判定

完全一致するURL文字列だけに依存すると仕様差異への耐性が低くなる可能性がある。

基本は正式なRelationship Typeを確認しつつ、末尾が以下であることも安全に判定できるようにする。

```text
/slide
/notesSlide
```

### XML名前空間

`querySelector("a\\:t")`だけに依存しない。

可能な限りローカル名または`getElementsByTagNameNS`を利用し、接頭辞の違いに耐えられるようにする。

### ユーザー編集の管理

`originalNotes`と`editedNotes`を分ける。

これにより将来的に次の機能を追加しやすくする。

- 編集を元に戻す
- 変更済み表示
- 元ノートとの差分表示

初期版で「元に戻す」ボタンを各スライドに付けてもよい。

### 保存時のBlob

生成したBlob URLはダウンロード後に`URL.revokeObjectURL`で解放する。

---

## 20. 最終目標

業務資料や講演資料を扱うユーザーが、PowerPointをアップロードすることなく、安全かつ簡単に発表者ノートを取り出し、Word文書として再利用できるWebアプリを完成させる。

「動くデモ」ではなく、ローカル環境で実用でき、静的ホスティングへ配置可能な初期製品として実装すること。
